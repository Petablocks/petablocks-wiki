# BookStack is configured via docker-compose on PETABLOCKS-FEA.
# Set these in /opt/petablocks/.env on the FEA VM.
# See petablocks-infra repo for the full docker-compose.yml.

WIKI_DB_USER=bookstack
WIKI_DB_PASSWORD=change_me_strong_password
